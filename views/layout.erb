<!doctype html>
<html lang="en" class="h-full bg-white">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/app.css" rel="stylesheet">
    <title>RodaDemo<% if @page_title %> - <%= @page_title %><% end %></title>
  </head>
  <body class="h-full">
    <nav class="navbar" role="navigation">
      <div class="container">
        <a class="navbar-brand" href="/">RodaDemo</a>
      </div>
    </nav>

    <div class="" id="content">
      <% if flash['notice'] %>
      <% end %>
      <% if flash['error'] %>
      <% end %>
      <% if @page_title %>
        <h1><%= @page_title %></h1>
      <% end %>
      <%== yield %>
    </div>
  </body>
</html>
